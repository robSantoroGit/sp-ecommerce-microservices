spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  
  jpa:
    hibernate:
      ddl-auto: none  # CRITICAL per Spring Boot 4
    show-sql: false
  
  sql:
    init:
      mode: always  # Esegue schema.sql